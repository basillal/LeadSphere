import React from 'react';
import Typography from '@mui/material/Typography';

const SourceManagement = () => {
    return (
        <Typography variant="h4">Source Management</Typography>
    );
};

export default SourceManagement;

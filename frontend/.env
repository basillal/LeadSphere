# ==============================================================================
# LEADSPHERE FRONTEND CONFIGURATION
# ==============================================================================

# VITE_API_URL: Leave empty in local development so requests go through the Vite proxy.
# That keeps API calls on the same origin and avoids duplicate CORS preflight traffic.
VITE_API_URL=
